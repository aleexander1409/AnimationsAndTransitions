<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.firstproy.alexanderserafin.animationsandtransitionsdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TransitionsActivity"
            android:theme="@style/AppTheme.Transitions" />
        <activity
            android:name=".TransitionOneActivity"
            android:theme="@style/AppTheme.Transitions" />
        <activity
            android:name=".ImageTransitionFullActivity"
            android:theme="@style/AppTheme.Transitions" />
        <activity
            android:name=".CircleRevealActivity"
            android:theme="@style/AppTheme.Transitions" />
        <activity android:name=".RecyclerAnimationActivity" />
        <activity android:name=".ConstraintsAnimationActivity" />
        <activity android:name=".SpringAnimationActivity"></activity>
    </application>

</manifest>